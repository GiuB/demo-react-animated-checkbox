import styled from 'styled-components'

export const CreditsWrapper = styled.div.attrs((props) => ({}))`
  max-width: 50rem;
  overflow: hidden;
  position: relative;
  margin: 5rem auto 0;

  h1 {
    margin-bottom: 1rem;
  }

  .inner {
    max-width: 99.2rem;
    margin: 1rem auto 0;
  }
`
