import styled from 'styled-components'

export const CarouselWrapper = styled.div.attrs((props) => ({}))`
  max-width: 190rem;
  overflow: hidden;
  position: relative;
  margin: 5rem auto 0;

  .inner {
    max-width: 99.2rem;
    margin: 1rem auto 0;
  }
`
